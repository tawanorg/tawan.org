import { Theme } from "@chakra-ui/react"

export type IFonts = Pick<Theme, 'fonts'>['fonts']

export type IFrontWeights = Pick<Theme, 'fontWeights'>['fontWeights'];

export type IBreakpoints = Pick<Theme, 'breakpoints'>['breakpoints'];

